/// Utility class to store a custom router configuration for navigation fallback.
class RouterConfigStorage {
  /// Static variable to hold a user-defined router configuration.
  /// Users can set this if the automatic detection of routerConfig from MaterialApp fails.
  static dynamic customRouterConfig;
} 